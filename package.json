{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && netlify deploy -d dist/ --prod",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.17.3"
  }
}
