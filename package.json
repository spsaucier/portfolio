{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && netlify deploy -d dist/ --prod",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.0.1"
  },
  "dependencies": {
    "@astrojs/netlify": "^6.0.0"
  }
}
